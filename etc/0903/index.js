// 컴퓨터가 랜덤 숫자 3개를 출력한다 (중복 x)
const comSel = parseInt(Math.random() * 3 + 1);

function start() {
  playerSel = prompt(
    `${count}\n숫자 3개를 선택해주세요\n중복숫자 금지\n 최대 : ${9} / 횟수 : ${count}`
  );
}

let comNb = console.log(Math.random(9));
comNb = (userNb[0] != userNb[1]) != arr[2];

const userNb = ["1", "2", "3", "4", "5", "6", "7", "8", "9"];

const ball = console.log("BALL");
// ball은 넘버들 중 배열이 다르면 할당
const out = console.log("OUT");
// OUT은 넘버가 다르면 할당

const comNum1 = parseInt(Math.random() * 9) + 1;

const comNum2 = parseInt(Math.random() * 9) + 1;

const comNum3 = parseInt(Math.random() * 9) + 1;

comNum1 = console.log(parseInt(Math.random() * 9) + 1);

let comNum2 = 0;
while (comNum1 != comNum2) {
  console.log(parseInt(Math.random() * 9) + 1);
  break;
}

let comNum3 = 0;
while (comNum3 != comNum2 || comNum3 != comNum1) {
  console.log(parseInt(Math.random() * 9) + 1);
}
